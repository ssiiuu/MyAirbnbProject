import React from 'react';
import { RectResult } from '@reactour/utils';
import { StylesObj } from './styles';
declare const Popover: React.FC<PopoverProps>;
export default Popover;
export declare type PopoverProps = {
    sizes: RectResult;
    children?: React.ReactNode;
    position?: PositionType;
    padding?: number | [number, number];
    styles?: StylesObj;
    className?: string;
    refresher?: any;
};
export declare type PositionType = Position | ((postionsProps: PositionProps) => Position);
export declare type PositionProps = RectResult & {
    windowWidth: number;
    windowHeight: number;
};
export declare type Position = 'top' | 'right' | 'bottom' | 'left' | 'center' | [number, number];
